'use strict'
import 'isomorphic-fetch'

import * as chai from 'chai'
//import 'fetch-mock/src/server'
//import * as mockery from 'mockery'

chai.should()

describe('fetchPosts', () => {
  it.skip('should fetch content from github', ()=> {
    console.log('stuff')
  })
})
